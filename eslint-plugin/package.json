{
  "name": "@graphql-fragment-import/eslint-plugin",
  "version": "0.1.0",
  "scripts": {
    "test": "mocha --recursive tests",
    "test:debug": "mocha --inspect-brk --recursive tests"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "eslint": "^7.9.0",
    "fixturify-project": "^2.1.0"
  }
}
